# Doorstep Laundry Marketplace — Python dependencies
#
# Local development (API + tests + tooling):
#   pip install -r requirements.txt
#
# Production only:
#   pip install -r requirements/prod.txt
#
# Base runtime only (FastAPI, DB, auth, storefront uploads via python-multipart):
#   pip install -r requirements/base.txt

-r requirements/dev.txt
