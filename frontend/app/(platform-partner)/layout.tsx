import { PlatformPartnerShell } from '@/components/layout/platform-partner-shell';

export default function PlatformPartnerLayout({ children }: { children: React.ReactNode }) {
  return <PlatformPartnerShell>{children}</PlatformPartnerShell>;
}
